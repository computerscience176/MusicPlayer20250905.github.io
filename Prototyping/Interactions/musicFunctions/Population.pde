/*population
 -divPopulation
 -colourPopulation
 */
//
void divPopulation () {
  topPanelX = appWidth * 0/16;
  topPanelY = appHeight * 0/24;
  topPanelWidth = appWidth * 16/16;
  topPanelHeight = appHeight * 1/24;
  rect(topPanelX, topPanelY, topPanelWidth, topPanelHeight);

  logoX = appWidth * 0.2/16;
  logoY = appHeight * 0/24;
  logoWidth = appWidth * 1/16;
  logoHeight = appHeight * 1/24;
  rect(logoX, logoY, logoWidth, logoHeight);

  websiteNameX = appWidth * 1.2/16;
  websiteNameY = appHeight * 0/24;
  websiteNameWidth = appWidth * 4.8/16;
  websiteNameHeight = appHeight * 1/24;
  rect(websiteNameX, websiteNameY, websiteNameWidth, websiteNameHeight);

  searchIconX = appWidth * 6/16;
  searchIconY = appHeight * 0/24;
  searchIconWidth = appWidth * 0.5/16;
  searchIconHeight = appHeight * 1/24;
  rect(searchIconX, searchIconY, searchIconWidth, searchIconHeight);

  searchX = appWidth * 6.8/16;
  searchY = appHeight * 0/24;
  searchWidth = appWidth * 4/16;
  searchHeight = appHeight * 1/24;
  rect(searchX, searchY, searchWidth, searchHeight);

  profileX = appWidth * 10.8/16;
  profileY = appHeight * 0/24;
  profileWidth = appWidth * 1.2/16;
  profileHeight = appHeight * 1/24;
  rect(profileX, profileY, profileWidth, profileHeight);

  accountX = appWidth * 12/16;
  accountY = appHeight * 0/24;
  accountWidth = appWidth * 2/16;
  accountHeight = appHeight * 1/24;
  rect(accountX, accountY, accountWidth, accountHeight);



  threeX = appWidth * 14.6/16;
  threeY = appHeight * 0/24;
  threeWidth = appWidth * 1.5/16;
  threeHeight = appHeight * 1/24;
  rect(threeX, threeY, threeWidth, threeHeight);

  rect(threeX + threeWidth* 1/4, threeY + threeHeight* 1/8, threeWidth*1/2, threeHeight*1.5/8);
  rect(threeX + threeWidth* 1/4, threeY + threeHeight* 2.9/8, threeWidth*1/2, threeHeight*1.5/8);
  rect(threeX + threeWidth* 1/4, threeY + threeHeight* 4.9/8, threeWidth*1/2, threeHeight*1.5/8);

  imageX = appWidth * 1/16;
  imageY = appHeight * 1.5/24;
  imageWidth = appWidth * 14/16;
  imageHeight = appHeight * 6.5/24;
  rect(imageX, imageY, imageWidth, imageHeight);

  addX = appWidth * 13/16;
  addY = appHeight * 6.5/24;
  addWidth = appWidth * 2/16;
  addHeight = appHeight * 1.5/24;
  rect(addX, addY, addWidth, addHeight);

  titleX = appWidth * 3/16;
  titleY = appHeight * 8.5/24;
  titleWidth = appWidth * 10/16;
  titleHeight = appHeight * 2/24;
  rect(titleX, titleY, titleWidth, titleHeight);

  textboxX = appWidth * 1/16;
  textboxY = appHeight * 11/24;
  textboxWidth = appWidth * 11.2/16;
  textboxHeight = appHeight * 9/24;
  rect(textboxX, textboxY, textboxWidth, textboxHeight);

  aboutX = appWidth * 1/16;
  aboutY = appHeight * 11/24;
  aboutWidth = appWidth * 2.3/16;
  aboutHeight = appHeight * 1/24;
  rect(aboutX, aboutY, aboutWidth, aboutHeight);

  byX = appWidth * 1/16;
  byY = appHeight * 12/24;
  byWidth = appWidth * 6/16;
  byHeight = appHeight * 0.5/24;
  rect(byX, byY, byWidth, byHeight);

  lyricsX = appWidth * 1.5/16;
  lyricsY = appHeight * 12.5/24;
  lyricsWidth = appWidth * 10.4/16;
  lyricsHeight = appHeight * 7/24;
  rect(lyricsX, lyricsY, lyricsWidth, lyricsHeight);

  iconBoxX = appWidth * 13/16;
  iconBoxY = appHeight * 11/24;
  iconBoxWidth = appWidth * 2/16;
  iconBoxHeight = appHeight * 9/24;
  rect(iconBoxX, iconBoxY, iconBoxWidth, iconBoxHeight);

  playlistX = appWidth * 13/16;
  playlistY = appHeight * 11/24;
  playlistWidth = appWidth * 2/16;
  playlistHeight = appHeight * 1.5/24;
  rect(playlistX, playlistY, playlistWidth, playlistHeight);

  volumeX = appWidth * 13/16;
  volumeY = appHeight * 12.5/24;
  volumeWidth = appWidth * 2/16;
  volumeHeight = appHeight * 1.5/24;
  rect(volumeX, volumeY, volumeWidth, volumeHeight);

  arabicX = appWidth * 13/16;
  arabicY = appHeight * 14/24;
  arabicWidth = appWidth * 2/16;
  arabicHeight = appHeight * 1.5/24;
  rect(arabicX, arabicY, arabicWidth, arabicHeight);

  englishX = appWidth * 13/16;
  englishY = appHeight * 15.5/24;
  englishWidth = appWidth * 2/16;
  englishHeight = appHeight * 1.5/24;
  rect(englishX, englishY, englishWidth, englishHeight);

  customizeX = appWidth * 13/16;
  customizeY = appHeight * 17/24;
  customizeWidth = appWidth * 2/16;
  customizeHeight = appHeight * 1.5/24;
  rect(customizeX, customizeY, customizeWidth, customizeHeight);

  notesX = appWidth * 13/16;
  notesY = appHeight * 18.5/24;
  notesWidth = appWidth * 2/16;
  notesHeight = appHeight * 1.5/24;
  rect(notesX, notesY, notesWidth, notesHeight);

  rectanglearoundtimeX = appWidth * 1/16;
  rectanglearoundtimeY = appHeight * 20.4/24;
  rectanglearoundtimeWidth = appWidth * 14.2/16;
  rectanglearoundtimeHeight = appHeight * 2.5/24;
  rect(rectanglearoundtimeX, rectanglearoundtimeY, rectanglearoundtimeWidth, rectanglearoundtimeHeight);

  previousX = appWidth * 1/16;
  previousY = appHeight * 20.4/24;
  previousWidth = appWidth * 1.4/16;
  previousHeight = appHeight * 2.5/24;
  rect(previousX, previousY, previousWidth, previousHeight);

  rect(previousX + previousWidth*1/4, previousY + previousHeight*1/4, previousWidth*1/8, previousHeight*1/2);
  triangle(previousX + previousWidth*3/8, previousY + previousHeight*1/2,
    previousX + previousWidth*3/4, previousY + previousHeight*1/4,
    previousX + previousWidth*3/4, previousY + previousHeight*3/4);

  playX = appWidth * 2.4/16;
  playY = appHeight * 20.4/24;
  playWidth = appWidth * 1.9/16;
  playHeight = appHeight * 2.5/24;
  rect(playX, playY, playWidth, playHeight);

  triangle(playX + playWidth*1/4, playY + playHeight*1/4,
    playX + playWidth*1/4, playY + playHeight*3/4,
    playX + playWidth*3/4, playY + playHeight*1/2);

  forwordX = appWidth * 4.3/16;
  forwordY = appHeight * 20.4/24;
  forwordWidth = appWidth * 1.4/16;
  forwordHeight = appHeight * 2.5/24;
  rect(forwordX, forwordY, forwordWidth, forwordHeight);

  triangle(forwordX + forwordWidth*1/4, forwordY + forwordHeight*1/4,
    forwordX + forwordWidth*1/4, forwordY + forwordHeight*3/4,
    forwordX + forwordWidth*5/8, forwordY + forwordHeight*1/2);
  rect(forwordX + forwordWidth*5/8, forwordY + forwordHeight*1/4,
    forwordWidth*1/8, forwordHeight*1/2);

  timeBoxX = appWidth * 6/16;
  timeBoxY = appHeight * 21/24;
  timeBoxWidth = appWidth * 8.2/16;
  timeBoxHeight = appHeight * 1.5/24;
  rect(timeBoxX, timeBoxY, timeBoxWidth, timeBoxHeight);

  timeX = appWidth * 6.3/16;
  timeY = appHeight * 21.5/24;
  timeWidth = appWidth * 7.7/16;
  timeHeight = appHeight * 0.3/24;
  rect(timeX, timeY, timeWidth, timeHeight);

  time1X = appWidth * 6.3/16;
  time1Y = appHeight * 21.8/24;
  time1Width = appWidth * 1/16;
  time1Height = appHeight * 0.5/24;
  rect(time1X, time1Y, time1Width, time1Height);

  time2X = appWidth * 13/16;
  time2Y = appHeight * 21.8/24;
  time2Width = appWidth * 1/16;
  time2Height = appHeight * 0.5/24;
  rect(time2X, time2Y, time2Width, time2Height);

  more1X = appWidth * 14.6/16;
  more1Y = appHeight * 20.4/24;
  more1Width = appWidth * 0.5/16;
  more1Height = appHeight * 0.87/24;
  rect(more1X, more1Y, more1Width, more1Height);

  more2X = appWidth * 14.6/16;
  more2Y = appHeight * 21.2/24;
  more2Width = appWidth * 0.5/16;
  more2Height = appHeight * 0.87/24;
  rect(more2X, more2Y, more2Width, more2Height);

  more3X = appWidth * 14.6/16;
  more3Y = appHeight * 22/24;
  more3Width = appWidth * 0.5/16;
  more3Height = appHeight * 0.87/24;
  rect(more3X, more3Y, more3Width, more3Height);

  //triangle(moreX + moreWidth*1/4, moreY + moreHeight*1/4, moreX + moreWidth*1/4, moreY + moreHeight*3/4, moreX + moreWidth*1/2, moreY + moreHeight*1/2);
  //Fast forward that appears when you click more.
  //triangle(moreX + moreWidth*1/2, moreY + moreHeight*1/4, moreX + moreWidth*1/2, moreY + moreHeight*3/4, moreX + moreWidth*3/4, moreY + moreHeight*1/2);
}//END DIV population

void colourPopulation () {
  //CANVAS: default background and ink
  resetBackgroundDay = #F5F5F5;  // Canvas background
  resetInkDay = charcoal;         // Ink / text
  resetBackgroundNight = #121212;
  resetInkNight = #E5E7EB;        // Light ink for night
  white = #FFFFFF;
  black = #000000;
  charcoal = #111827;

  resetBackgroundDay = #F5F5F5;
  resetInkDay = charcoal;
  resetBackgroundNight = #121212;
  resetInkNight = #E5E7EB;

  softNavy = #1E3A8A;
  lightBlue = #3B82F6;
  offWhite = #E5E7EB;
  cream = #F3F4F6;
  boldRed = #EF4444;
}
