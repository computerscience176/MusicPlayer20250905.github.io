fullScreen(displayWidth); //displayWidth //displayHeight
int appWidth = displayWidth;
int appHeight = displayHeight;

float logoX = appWidth * 0.2;
float logoY = appHeight * 1;
float logoWidth = appWidth * 1;
float logoHeight = appHeight * 1;
rect(logoX, logoY, logoWidth, logoHeight);

float websiteNameX = appWidth * 1.2;
float websiteNameY = appHeight * 1;
float websiteNameWidth = appWidth * 4.8;
float websiteNameHeight = appHeight * 1;
rect(websiteNameX, websiteNameY, websiteNameWidth, websiteNameHeight);

float searchIconX = appWidth * 6;
float searchIconY = appHeight * 1;
float searchIconWidth = appWidth * 0.5;
float searchIconHeight = appHeight * 1;
rect(searchIconX, searchIconY, searchIconWidth, searchIconHeight);

float searchX = appWidth * 6.8;
float searchY = appHeight * 1;
float searchWidth = appWidth * 4;
float searchHeight = appHeight * 1;
rect(searchX, searchY, searchWidth, searchHeight);

float profileX = appWidth * 10.8;
float profileY = appHeight * 1;
float profileWidth = appWidth * 1.2;
float profileHeight = appHeight * 1;
rect(profileX, profileY, profileWidth, profileHeight);

float accountX = appWidth * 12;
float accountY = appHeight * 1;
float accountWidth = appWidth * 2;
float accountHeight = appHeight * 1;
rect(accountX, accountY, accountWidth, accountHeight);

float threeX = appWidth * 14.6;
float threeY = appHeight * 1;
float threeWidth = appWidth * 1.5;
float threeHeight = appHeight * 1;
rect(threeX, threeY, threeWidth, threeHeight);

float imageX = appWidth * 1;
float imageY = appHeight * 1.5;
float imageWidth = appWidth * 14;
float imageHeight = appHeight * 6.5;
rect(imageX, imageY, imageWidth, imageHeight);

float addX = appWidth * 13.3;
float addY = appHeight * 6.5;
float addWidth = appWidth * 1.8;
float addHeight = appHeight * 1.6;
rect(addX, addY, addWidth, addHeight);

float titleX = appWidth * 3;
float titleY = appHeight * 6.5;
float titleWidth = appWidth * 10;
float titleHeight = appHeight * 2;
rect(titleX, titleY, titleWidth, titleHeight);

float aboutX = appWidth * 1;
float aboutY = appHeight * 11;
float aboutWidth = appWidth * 2.3;
float aboutHeight = appHeight * 1;
rect(aboutX, aboutY, aboutWidth, aboutHeight);

float byX = appWidth * 1;
float byY = appHeight * 12;
float byWidth = appWidth * 6;
float byHeight = appHeight * 0.5;
rect(byX, byY, byWidth, byHeight);

float lyricsX = appWidth * 1.5;
float lyricsY = appHeight * 12.5;
float lyricsWidth = appWidth * 10.4;
float lyricsHeight = appHeight * 7;
rect(lyricsX, lyricsY, lyricsWidth, lyricsHeight);

float textboxX = appWidth * 1;
float textboxY = appHeight * 11;
float textboxWidth = appWidth * 11.2;
float textboxHeight = appHeight * 9;
rect(textboxX, textboxY, textboxWidth, textboxHeight);

float playlistX = appWidth * 13;
float playlistY = appHeight * 11;
float playlistWidth = appWidth * 2;
float playlistHeight = appHeight * 1.5;
rect(playlistX, playlistY, playlistWidth, playlistHeight);

float volumeX = appWidth * 13;
float volumeY = appHeight * 12.5;
float volumeWidth = appWidth * 2;
float volumeHeight = appHeight * 1.5;
rect(volumeX, volumeY, volumeWidth, volumeHeight);

float arabicX = appWidth * 13;
float arabicY = appHeight * 14;
float arabicWidth = appWidth * 2;
float arabicHeight = appHeight * 1.5;
rect(arabicX, arabicY, arabicWidth, arabicHeight);

float englishX = appWidth * 13;
float englishY = appHeight * 15.5;
float englishWidth = appWidth * 2;
float englishHeight = appHeight * 1.5;
rect(englishX, englishY, englishWidth, englishHeight);

float customizeX = appWidth * 13;
float customizeY = appHeight * 17;
float customizeWidth = appWidth * 2;
float customizeHeight = appHeight * 1.5;
rect(customizeX, customizeY, customizeWidth, customizeHeight);

float notesX = appWidth * 13;
float notesY = appHeight * 18.5;
float notesWidth = appWidth * 2;
float notesHeight = appHeight * 1.5;
rect(notesX, notesY, notesWidth, notesHeight);

float iconBoxX = appWidth * 13;
float iconBoxY = appHeight * 11;
float iconBoxWidth = appWidth * 2;
float iconBoxHeight = appHeight * 9;
rect(iconBoxX, iconBoxY, iconBoxWidth, iconBoxHeight);

float previousX = appWidth * 1;
float previousY = appHeight * 20.4;
float previousWidth = appWidth * 1.4;
float previousHeight = appHeight * 2.5;
rect(previousX, previousY, previousWidth, previousHeight);

float playX = appWidth * 2.4;
float playY = appHeight * 20.4;
float playWidth = appWidth * 1.9;
float playHeight = appHeight * 2.5;
rect(playX, playY, playWidth, playHeight);

float forwordX = appWidth * 4.3;
float forwordY = appHeight * 20.4;
float forwordWidth = appWidth * 1.4;
float forwordHeight = appHeight * 2.5;
rect(forwordX, forwordY, forwordWidth, forwordHeight);

float timeBoxX = appWidth * 6;
float timeBoxY = appHeight * 21;
float timeBoxWidth = appWidth * 8.2;
float timeBoxHeight = appHeight * 1.5;
rect(timeBoxX, timeBoxY, timeBoxWidth, timeBoxHeight);

float timeX = appWidth * 6.3;
float timeY = appHeight * 21.5;
float timeWidth = appWidth * 7.5;
float timeHeight = appHeight * 03;
rect(timeX, timeY, timeWidth, timeHeight);

float time1X = appWidth * 6.3;
float time1Y = appHeight * 21.8;
float time1Width = appWidth * 1;
float time1Height = appHeight * 0.5;
rect(time1X, time1Y, time1Width, time1Height);

float time2X = appWidth * 13;
float time2Y = appHeight * 21.8;
float time2Width = appWidth * 1;
float time2Height = appHeight * 0.5;
rect(time2X, time2Y, time2Width, time2Height);

float rectanglearoundtimeX = appWidth * 1;
float rectanglearoundtimeY = appHeight * 20.4;
float rectanglearoundtimeWidth = appWidth * 14.2;
float rectanglearoundtimeHeight = appHeight * 2.5;
rect(rectanglearoundtimeX, rectanglearoundtimeY, rectanglearoundtimeWidth, rectanglearoundtimeHeight);

float more1X = appWidth * 14.3;
float more1Y = appHeight * 20.4;
float more1Width = appWidth * 0.5;
float more1Height = appHeight * 0.8;
rect(more1X, more1Y, more1Width, more1Height);

float more2X = appWidth * 14.3;
float more2Y = appHeight * 21.2;
float more2Width = appWidth * 0.5;
float more2Height = appHeight * 0.8;
rect(more2X, more2Y, more2Width, more2Height);

float more3X = appWidth * 14.3;
float more3Y = appHeight * 22;
float more3Width = appWidth * 0.5;
float more3Height = appHeight * 0.8;
rect(more3X, more3Y, more3Width, more3Height);

float bigboxX = appWidth ;
float bigboxY = appHeight ;
float bigboxWidth = appWidth ;
float bigboxHeight = appHeight ;
rect(bigboxX, bigboxY, bigboxWidth, bigboxHeight);
