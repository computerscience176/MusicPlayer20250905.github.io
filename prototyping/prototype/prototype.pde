fullScreen(displayWidth); //displayWidth //displayHeight
int appWidth = displayWidth;
int appHeight = displayHeight;

// Logo box
float logoX = appWidth * 0.5/24;
float logoY = appHeight * 1/16;
float logoWidth = appWidth * 1.5/24;
float logoHeight = appHeight * 1/16;
rect(logoX, logoY, logoWidth, logoHeight);

// Website name
float websiteNameX = appWidth * 2/24;
float websiteNameY = appHeight * 1/16;
float websiteNameWidth = appWidth * 5/24;
float websiteNameHeight = appHeight * 1/16;
rect(websiteNameX, websiteNameY, websiteNameWidth, websiteNameHeight);

// Search icon
float searchIconX = appWidth * 7/24;
float searchIconY = appHeight * 1/16;
float searchIconWidth = appWidth * 1/24;
float searchIconHeight = appHeight * 1/16;
rect(searchIconX, searchIconY, searchIconWidth, searchIconHeight);

// Search bar
float searchX = appWidth * 8/24;
float searchY = appHeight * 1/16;
float searchWidth = appWidth * 6/24;
float searchHeight = appHeight * 1/16;
rect(searchX, searchY, searchWidth, searchHeight);

// Profile box
float profileX = appWidth * 14/24;
float profileY = appHeight * 1/16;
float profileWidth = appWidth * 2/24;
float profileHeight = appHeight * 1/16;
rect(profileX, profileY, profileWidth, profileHeight);

// Account box
float accountX = appWidth * 16/24;
float accountY = appHeight * 1/16;
float accountWidth = appWidth * 3/24;
float accountHeight = appHeight * 1/16;
rect(accountX, accountY, accountWidth, accountHeight);

// Three-dot menu
float threeX = appWidth * 20/24;
float threeY = appHeight * 1/16;
float threeWidth = appWidth * 2/24;
float threeHeight = appHeight * 1/16;
rect(threeX, threeY, threeWidth, threeHeight);

// Image
float imageX = appWidth * 1/24;
float imageY = appHeight * 2/16;
float imageWidth = appWidth * 17/24;
float imageHeight = appHeight * 7/16;
rect(imageX, imageY, imageWidth, imageHeight);

// Add button
float addX = appWidth * 18/24;
float addY = appHeight * 6/16;
float addWidth = appWidth * 3/24;
float addHeight = appHeight * 2/16;
rect(addX, addY, addWidth, addHeight);

// Title
float titleX = appWidth * 3/24;
float titleY = appHeight * 6/16;
float titleWidth = appWidth * 12/24;
float titleHeight = appHeight * 2/16;
rect(titleX, titleY, titleWidth, titleHeight);

// About
float aboutX = appWidth * 1/24;
float aboutY = appHeight * 9/16;
float aboutWidth = appWidth * 4/24;
float aboutHeight = appHeight * 1/16;
rect(aboutX, aboutY, aboutWidth, aboutHeight);

// By
float byX = appWidth * 1/24;
float byY = appHeight * 10/16;
float byWidth = appWidth * 9/24;
float byHeight = appHeight * 0.5/16;
rect(byX, byY, byWidth, byHeight);

// Lyrics
float lyricsX = appWidth * 2/24;
float lyricsY = appHeight * 11/16;
float lyricsWidth = appWidth * 14/24;
float lyricsHeight = appHeight * 7/16;
rect(lyricsX, lyricsY, lyricsWidth, lyricsHeight);

// Textbox
float textboxX = appWidth * 1/24;
float textboxY = appHeight * 9/16;
float textboxWidth = appWidth * 15/24;
float textboxHeight = appHeight * 9/16;
rect(textboxX, textboxY, textboxWidth, textboxHeight);

// Playlist
float playlistX = appWidth * 18/24;
float playlistY = appHeight * 9/16;
float playlistWidth = appWidth * 3/24;
float playlistHeight = appHeight * 2/16;
rect(playlistX, playlistY, playlistWidth, playlistHeight);

// Volume
float volumeX = appWidth * 18/24;
float volumeY = appHeight * 11/16;
float volumeWidth = appWidth * 3/24;
float volumeHeight = appHeight * 2/16;
rect(volumeX, volumeY, volumeWidth, volumeHeight);

// Arabic
float arabicX = appWidth * 18/24;
float arabicY = appHeight * 13/16;
float arabicWidth = appWidth * 3/24;
float arabicHeight = appHeight * 2/16;
rect(arabicX, arabicY, arabicWidth, arabicHeight);

// English
float englishX = appWidth * 18/24;
float englishY = appHeight * 15/16;
float englishWidth = appWidth * 3/24;
float englishHeight = appHeight * 2/16;
rect(englishX, englishY, englishWidth, englishHeight);

// Customize
float customizeX = appWidth * 18/24;
float customizeY = appHeight * 17/16;
float customizeWidth = appWidth * 3/24;
float customizeHeight = appHeight * 2/16;
rect(customizeX, customizeY, customizeWidth, customizeHeight);

// Notes
float notesX = appWidth * 18/24;
float notesY = appHeight * 19/16;
float notesWidth = appWidth * 3/24;
float notesHeight = appHeight * 2/16;
rect(notesX, notesY, notesWidth, notesHeight);

// Icon box
float iconBoxX = appWidth * 18/24;
float iconBoxY = appHeight * 9/16;
float iconBoxWidth = appWidth * 3/24;
float iconBoxHeight = appHeight * 9/16;
rect(iconBoxX, iconBoxY, iconBoxWidth, iconBoxHeight);

// Previous
float previousX = appWidth * 1/24;
float previousY = appHeight * 13/16;
float previousWidth = appWidth * 2/24;
float previousHeight = appHeight * 3/16;
rect(previousX, previousY, previousWidth, previousHeight);

// Play
float playX = appWidth * 3/24;
float playY = appHeight * 13/16;
float playWidth = appWidth * 3/24;
float playHeight = appHeight * 3/16;
rect(playX, playY, playWidth, playHeight);

// Forward
float forwordX = appWidth * 6/24;
float forwordY = appHeight * 13/16;
float forwordWidth = appWidth * 2/24;
float forwordHeight = appHeight * 3/16;
rect(forwordX, forwordY, forwordWidth, forwordHeight);

// Time box
float timeBoxX = appWidth * 9/24;
float timeBoxY = appHeight * 14/16;
float timeBoxWidth = appWidth * 12/24;
float timeBoxHeight = appHeight * 2/16;
rect(timeBoxX, timeBoxY, timeBoxWidth, timeBoxHeight);

// Time bar
float timeX = appWidth * 9.5/24;
float timeY = appHeight * 14.5/16;
float timeWidth = appWidth * 11/24;
float timeHeight = appHeight * 0.3/16;
rect(timeX, timeY, timeWidth, timeHeight);

// Time left
float time1X = appWidth * 9.5/24;
float time1Y = appHeight * 15/16;
float time1Width = appWidth * 2/24;
float time1Height = appHeight * 0.5/16;
rect(time1X, time1Y, time1Width, time1Height);

// Time right
float time2X = appWidth * 19/24;
float time2Y = appHeight * 15/16;
float time2Width = appWidth * 2/24;
float time2Height = appHeight * 0.5/16;
rect(time2X, time2Y, time2Width, time2Height);

// Rectangle around time
float rectanglearoundtimeX = appWidth * 1/24;
float rectanglearoundtimeY = appHeight * 13/16;
float rectanglearoundtimeWidth = appWidth * 20/24;
float rectanglearoundtimeHeight = appHeight * 3/16;
rect(rectanglearoundtimeX, rectanglearoundtimeY, rectanglearoundtimeWidth, rectanglearoundtimeHeight);

// More buttons
float more1X = appWidth * 21/24;
float more1Y = appHeight * 13/16;
float more1Width = appWidth * 1/24;
float more1Height = appHeight * 0.5/16;
rect(more1X, more1Y, more1Width, more1Height);

float more2X = appWidth * 21/24;
float more2Y = appHeight * 14/16;
float more2Width = appWidth * 1/24;
float more2Height = appHeight * 0.5/16;
rect(more2X, more2Y, more2Width, more2Height);

float more3X = appWidth * 21/24;
float more3Y = appHeight * 15/16;
float more3Width = appWidth * 1/24;
float more3Height = appHeight * 0.5/16;
rect(more3X, more3Y, more3Width, more3Height);

// Big box
float bigboxX = appWidth * 0/24;
float bigboxY = appHeight * 0/16;
float bigboxWidth = appWidth * 24/24;
float bigboxHeight = appHeight * 16/16;
rect(bigboxX, bigboxY, bigboxWidth, bigboxHeight);
